# 经人工审阅的 Git 提交界定知识入库

为区分研究期间的候选判断与正式知识，用户确认：Agent 可以在工作区生成候选知识变更并执行校验，但只有用户审阅确认后，Agent 才能通过 Git 提交将其正式入库。该选择保留了逐轮人工审阅的成本，以避免将 Agent 的文件写入自动视为知识接受；后续恢复与查询流程需要识别尚未入库的候选变更。

后续确认：研究轨迹与任务进度在本地主动保存，本轮相关记录随知识入库集中审阅并一并提交；进入 Git 历史的研究进度不因此转化为正式知识判断。
